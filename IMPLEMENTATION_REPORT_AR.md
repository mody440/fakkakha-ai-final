# تقرير تنفيذ Fakkakha AI

التاريخ: 2026-09-22

## التحقق البرمجي المنفذ

مرّت أوامر `pnpm test` و`pnpm check` و`pnpm build` بعد التعديلات المرحلية. خط الاختبارات الحالي: 46 اختبارًا ناجحًا، والتحقق النحوي والبناء الإنتاجي ناجحان. لم يُنفذ تشغيل ضغط فعلي أو نشر staging لأن ذلك يحتاج بيئة ومفاتيح خارجية.

## الحالة حسب النقاط

| النقطة | الحالة | التفاصيل |
|---|---|---|
| 1. Error envelope موحد | تمت بالكامل | طبقة `withMetrics` توحد `code/message/requestId` وتضيف `x-request-id`. |
| 2. Skeleton وإعادة المحاولة | جزئيًا | الانتقالات الرئيسية تستخدم skeleton وحالة خدمة غير متاحة وزر إعادة محاولة؛ توجد حالات inline متخصصة لم تُوحّد كلها. |
| 3. فشل قاعدة البيانات صريح | تمت جزئيًا | مسارات RAG والملفات الأساسية لا تستخدم بيانات معاينة صامتة؛ أُزيل fallback التصنيف العام عند تعطل النموذج وأصبح 503 صريحًا. ما زالت مراجعة كل واجهة مرتبطة بقاعدة حية مطلوبة. |
| 4. كود المسابقة المركزي | تحتاج مدخلات خارجية | لا يوجد كود مسابقة في الأرشيف الحالي؛ لا يمكن بناء ترتيب/مسابقة حقيقية دون جدول وقواعد المنتج المطلوبة. |
| 5. ذاكرة الجلسة | تمت بالكامل | جلسات ورسائل محفوظة، مع idempotency لـ `turnId` واستئناف الجلسة. |
| 6. SSE | تمت جزئيًا | endpoint والعميل موجودان؛ يلزم اختبار متصفح/انقطاع اتصال فعلي في بيئة تشغيل. |
| 7. ملف المتعلم | تمت جزئيًا | العمر والصف والمادة واللغة موجودة في schema والواجهة، لكن تطبيق migration على Supabase فعليًا خارجي. |
| 8. rubric عمري ثابت | تمت بالكامل | rubric ثابت في `lib/learnerRubric.js` ويُمرر إلى المصنف والجلسة. |
| 9. classifier متعدد اللغات | جزئيًا | تحسن التصنيف ويدعم العربي/الإنجليزي/العامية، لكن benchmark لغوي واسع يحتاج بيانات تقييم حقيقية. |
| 10. طبي/قانوني/هندسي | جزئيًا | توجد guardrails وتحذيرات وسياق، لكن triage متخصص ومراجع بشرية يحتاج سياسة منتج ومختصين. لا ينبغي اعتبار النظام تشخيصًا أو استشارة قانونية. |
| 11. تحويل صوت ثم إجابة | جزئيًا | إدخال الصوت موجود؛ زر one-click الكامل يحتاج اختبار متصفح وتخزين صوت مناسب. |
| 12. قاموس مواد شامل | تحتاج مدخلات خارجية | لا يجوز اختلاق مناهج أو معايير؛ يلزم كتالوج رسمي متعدد الدول واللغات. |
| 13. الوضع الصارم للوحدة | تمت جزئيًا | الوضع الصارم يرفض الخروج من الوحدة، والمولد يقبل `unit/topics` والصف والعدد؛ معالجة نقص بنك الأسئلة تحتاج بنكًا حقيقيًا وموافقة UX مكتملة. |
| 14. مصحح مقالي rubric | تحتاج تنفيذ إضافي | غير موجود كمسار مستقل في الأرشيف الحالي؛ لا أعدّه منفذًا. |
| 15. workflow الأسئلة | تمت جزئيًا | `question_bank` بحالات draft/approved/rejected/needs_edit وواجهات API موجودة بلا مدرسين وهميين؛ واجهة مراجعة كاملة تحتاج حسابات وأدوار حقيقية. |
| 16. pipeline توليد الأسئلة | تمت جزئيًا | مولد الاختبارات يحفظ كل سؤال مولدًا كمسودة `draft` بعد الإنشاء، مع بقاء الاعتماد يدويًا. queue إنتاجية تحتاج تشغيلًا دائمًا. |
| 17. معيار رسمي للتعلم | تمت بنيويًا | جدول `learning_standards` جاهز للمعرّف والجهة والرابط دون اختلاق معيار. تعبئته خارجية. |
| 18. rate limit user/session/device وRedis | تمت جزئيًا | المسارات الأساسية تستخدم مفتاحًا مركبًا user+device+session، وRedis/Upstash مسار جاهز؛ يلزم تفعيل Redis أو تطبيق دالة SQL في المشروع الفعلي. |
| 19. quota يومي | جزئيًا | أُضيف migration `daily_quotas`، ولم يُربط كل endpoint بعد بعدّاد يومي تنفيذي. |
| 20. abuse detection | جزئيًا | guardrails وحدود ومعدل موجودة؛ scoring مستقل يحتاج سياسة ومراجعة false positives. |
| 21. token/cost accounting | غير مكتملة | أُضيف جدول `ai_usage` في migration، لكن لا يجوز تقدير تكلفة فعلية دون usage موثوق من مزود النموذج وتعرفة معتمدة. |
| 22. الخصوصية والشروط والتصدير | تمت جزئيًا | الصفحات وendpoint التصدير موجودة؛ موافقة ولي الأمر تحت 13 تحتاج مزود تحقق/سياسة قانونية حقيقية. |
| 23. Sentry وdashboard | تمت جزئيًا | Sentry يقرأ `SENTRY_DSN`، وأضيفت `public/dashboard.html` التي لا تخزن المفتاح؛ المقاييس المتاحة محدودة بما يرجعه `/api/metrics`. |
| 24. تدقيق الأسرار | تمت بالكامل بنيويًا | `.env.example`، عدم وضع مفاتيح في الواجهة، وفحص build يمنع ملفات `.env` ومؤشرات مفاتيح شائعة. |
| 25. Mobile-first/accessibility | تمت جزئيًا | واجهة بعرض هاتف، focus-visible وARIA وأزرار لوحة مفاتيح موجودة؛ تدقيق WCAG كامل وقارئ شاشة يحتاج فحص Lighthouse/axe فعلي. |
| 26. الدفع/Stripe | خارج النطاق | لم يُنفذ أي شيء خاص بالدفع أو Stripe. |
| 27. Home.tsx | غير منطبق | الأرشيف plain JS/HTML وليس React/TypeScript؛ لم أخترع تحويلًا كاملًا للإطار. |
| 28. code splitting | جزئيًا | lazy behavior موجود؛ لا يوجد bundler حديث لتقسيم إضافي. |
| 29. i18n كامل | جزئيًا | بنية i18n موجودة، لكن بعض النصوص العربية الجديدة ما زالت تحتاج إضافة قاموس English كامل. |
| 30. PWA offline/sync | جزئيًا | service worker وoffline engine موجودان؛ طابور محاولات دائم ومزامنة conflict-safe يحتاج تنفيذًا واختبارًا فعليًا. |
| 31. الصوت المباشر بدل data URL | غير مكتملة | الضغط موجود، لكن endpoint الحالي يستقبل base64؛ رفع مباشر يحتاج storage bucket وسياسة وحسابات خارجية. |
| 32. لوحة المعلم | تحتاج مدخلات خارجية | لا يجوز إنشاء فصول أو مدرسين وهميين؛ تحتاج بيانات وصلاحيات مدارس حقيقية. |
| 33. المجتمع والمسابقات والترتيب | تحتاج تنفيذ/بيانات خارجية | لا يوجد تخزين حقيقي مكتمل في الأرشيف؛ يحتاج schema وسياسات moderation وحسابات حقيقية. |
| 34. اختبارات API | جزئيًا | 46 اختبارًا وتغطية loadability للمسارات؛ أضيف endpoint البحث إلى surface test، لكن ليس كل endpoint مغطى سلوكيًا بالكامل. |
| 35. Playwright E2E | جزئيًا | scaffold موجود؛ يحتاج تشغيل browser وبيئة Supabase/AI فعلية ومسارات عربي/English. |
| 36. k6 load test | تمت كقالب | ملف k6 جاهز؛ التنفيذ على staging خارج النطاق. |
| 37. مولد اختبارات ديناميكي | تمت جزئيًا | حسب الصف والوحدة/الموضوع والعدد والصعوبة، مع منع الخروج من الوحدة ومسودات مراجعة. لا توجد وفرة محتوى منهجي بدون مصادر مرخصة. |
| 38. البحث الحقيقي والاستشهادات | تمت جزئيًا | أضيف `/api/research` وشاشة بحث تعرض HTTPS URL وdomain وsnippet ووقت الاسترجاع فقط من مزود مهيأ. يلزم إدخال `SEARCH_PROVIDER_URL/API_KEY` وتشغيل مزود حقيقي؛ لا تظهر نتائج وهمية عند غياب الإعداد. |

## متطلبات خارجية صريحة

- الدفع/Stripe: خارج النطاق بطلب المستخدم.
- المناهج والكتب والتراخيص والمعايير الرسمية: تحتاج ملفات/تراخيص/دولة مستهدفة ومراجعة حقوق.
- المدرسون والمدارس والفصول: تحتاج حسابات وأدوار وبيانات حقيقية.
- البحث الحي: يحتاج اختيار مزود موثوق ومفتاح خادم، ثم اختبار شروطه وسياساته.
- Supabase migrations وSentry وRedis ومفتاح النموذج: تحتاج تطبيقًا في المشروع الفعلي.
- staging/production وPlaywright وk6 وLighthouse: تحتاج عنوان بيئة منشورة وبيانات اختبار، لذلك لم أزعم نجاحها محليًا.

## ملاحظات أمان مهمة

لا يمكن ضمان «عدم وجود أي نقطة ضعف» لأي نظام، خصوصًا نظام يستخدم نموذجًا احتماليًا وخدمات خارجية. ما تم فعله هو تقليل نقاط الضعف المعروفة، منع fallback المضلل، رفض مصادر غير HTTPS، عدم تخزين الأسرار في الواجهة، وتسجيل حالات الفشل بدل إخفائها. الاستخدام الطبي والقانوني والهندسي يجب أن يبقى معلوماتيًا مع إحالة لمختص، وليس تشخيصًا أو رأيًا مهنيًا ملزمًا.
